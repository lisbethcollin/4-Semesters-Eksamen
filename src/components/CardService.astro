---

---

<section x-data="{ alleservices: []}" x-init="alleservices = await (await fetch('services.json')).json();" class="flex gap-14 max-w-min m-auto ">
    <template x-for="services in alleservices" :key="services.id">
        <ul>
            <li>
                <article class="flex flex-col place-content-between border-2 border-orange-500 hover:bg-orange-200 rounded-3xl py-4 px-6 w-64 h-96">
                    <div>
                        <p x-text="services.servicenummer" class="font-albert text-sm"></p>
                        <h4 x-text="services.servicetitle" class="font-albert text-lg font-medium uppercase"></h4>
                    </div>
                    <div>
                        <p x-text="services.servicebeskrivelse" class="font-albert italic font-light"></p>
                        <hr class="border-blue-500 mt-2 mb-4">
                        <ul class="list-disc pl-6 space-y-2">
                            <template x-for="punkt in services.servicepunkt" :key="punkt">
                                <li x-text="punkt" class="font-albert text-sm"></li>
                            </template>
                        </ul>
                    </div>
                </article>
            </li>
        </ul>
    </template>
</section>